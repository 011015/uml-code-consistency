import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  components: {
    Layout: {
      siderBg: "#ffffff",
      headerBg: "#ffffff",
      headerHeight: 50,
    },
  },
};

export default theme;
